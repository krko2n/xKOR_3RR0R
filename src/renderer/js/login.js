(function xkorLogin(){'use strict';var overlay=document.getElementById('login-overlay');if(!overlay)return;var userIn=document.getElementById('login-user');var passIn=document.getElementById('login-pass');var btn=document.getElementById('login-btn');var errDiv=document.getElementById('login-error');function clearErr(){errDiv.textContent='';errDiv.classList.remove('shake');}userIn.addEventListener('input',clearErr);passIn.addEventListener('input',clearErr);function showError(msg){errDiv.textContent=msg;errDiv.classList.remove('shake');void errDiv.offsetWidth;errDiv.classList.add('shake');}function attemptLogin(){var username=(userIn.value||'').trim();var password=passIn.value||'';if(!username){showError('USERNAME REQUIRED');userIn.focus();return;}if(!password){showError('PASSWORD REQUIRED');passIn.focus();return;}btn.disabled=true;btn.textContent='AUTHENTICATING...';clearErr();fetch('http://localhost:3000/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:username,password:password})}).then(function(r){return r.json();}).then(function(d){if(d.ok){overlay.style.transition='opacity 0.5s ease';overlay.style.opacity='0';setTimeout(function(){overlay.classList.add('hidden');window.dispatchEvent(new CustomEvent('xkor-auth-ok',{detail:{username:username}}));},500);}else{showError('ACCESS DENIED');passIn.value='';passIn.focus();btn.disabled=false;btn.textContent='AUTHENTICATE';}}).catch(function(){showError('BACKEND UNREACHABLE');setTimeout(function(){btn.disabled=false;btn.textContent='AUTHENTICATE';},2000);});}btn.addEventListener('click',attemptLogin);passIn.addEventListener('keydown',function(e){if(e.key==='Enter')attemptLogin();});userIn.addEventListener('keydown',function(e){if(e.key==='Enter')passIn.focus();});userIn.focus();})();